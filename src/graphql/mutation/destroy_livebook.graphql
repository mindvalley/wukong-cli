mutation DestroyLivebook(
  $application: String!
  $namespace: String!
  $version: String!
) {
  destroyLivebook(
    application: $application
    namespace: $namespace
    version: $version
  ) {
    createdAt
    deletedAt
    podIp
    labels
    name
    namespace
    password
    podState
    ready
    restartCount
    url
  }
}
