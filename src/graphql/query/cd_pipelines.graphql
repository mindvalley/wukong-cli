query CdPipelinesQuery($application: String!) {
  cdPipelines(application: $application) {
    deployedBy
    deployedRef
    buildArtifact
    environment
    enabled
    lastDeployment
    name
    status
    version
  }
}
