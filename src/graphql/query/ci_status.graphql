query CiStatusQuery($repoUrl: String!, $branch: String!) {
  ciStatus(repoUrl: $repoUrl, branch: $branch) {
    name
    buildUrl
    buildDuration
    buildNumber
    commitAuthor
    commitId
    commitMsg
    result
    timestamp
    totalDuration
    waitDuration
  }
}
