query PipelinesQuery {
  pipelines {
    __typename
    ... on MultiBranchPipeline {
      name
      lastSucceededAt
      lastDuration
      lastFailedAt
    }
    ... on Job {
      name
      lastSucceededAt
      lastDuration
      lastFailedAt
    }
  }
}
