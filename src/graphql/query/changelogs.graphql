query ChangelogsQuery($application: String!, $namespace: String!, $version:
String!, $buildNumber: Int!) {
  changelogs(application: $application, namespace: $namespace, version:
  $version, buildNumber: $buildNumber) {
    id
    message
  }
}
