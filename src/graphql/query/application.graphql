query ApplicationQuery($name: String!) {
  application(name: $name) {
    name
    basicInfo {
      deploymentTarget
      deploymentStrategy
      links {
          title
          url
      }
    }
  }
}
