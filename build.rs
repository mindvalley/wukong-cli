// use crate::commands::CommandGroup;
// use clap::{Clap, CommandFactory, Parser};

// include!("src/clap_app.rs");

fn main() {
    // let mut app = ClapApp::command();

    // todo!("generate the completion scripts!");
}
