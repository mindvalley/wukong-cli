query ApplicationSecretsQuery(
  $application: String!
  $namespace: String!
  $path: String!
) {
  applicationSecrets(
    application: $application
    namespace: $namespace
    path: $path
  ) {
    key
    value
  }
}
