query CdPipelineStatusQuery($application: String!, $namespace: String!, $version:
String!) {
  cdPipeline(application: $application, namespace: $namespace, version: $version) {
    lastSuccessfullyDeployedArtifact
    status
  }
}
